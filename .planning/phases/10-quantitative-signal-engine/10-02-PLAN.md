---
phase: 10-quantitative-signal-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - gods-eye/backend/app/engine/quant_backtest.py
  - gods-eye/backend/app/api/routes.py
  - gods-eye/backend/app/api/schemas.py
autonomous: true
requirements:
  - QUANT-01
  - QUANT-03
  - QUANT-05

must_haves:
  truths:
    - "GET /api/signal/quant/{instrument}/{date} returns score 0-100 with per-factor breakdown"
    - "POST /api/backtest/quant-run over 1 year of stored data completes in under 10 seconds"
    - "The backtest loop makes zero network calls — reads only from SQLite"
    - "Score > 50 returns BUY or SELL; score at or below 50 returns HOLD"
    - "QuantBacktestEngine is fully independent of BacktestEngine — no Orchestrator, no LLM"
  artifacts:
    - path: "gods-eye/backend/app/engine/quant_backtest.py"
      provides: "QuantBacktestEngine.run_quant_backtest() — pure SQLite day loop"
      exports: ["QuantBacktestEngine", "QuantBacktestDayResult", "QuantBacktestRunResult", "quant_backtest_engine"]
    - path: "gods-eye/backend/app/api/schemas.py"
      provides: "QuantSignalResponse and QuantBacktestRunResponse Pydantic schemas appended"
    - path: "gods-eye/backend/app/api/routes.py"
      provides: "GET /api/signal/quant/{instrument}/{date} and POST /api/backtest/quant-run on protected_router"
  key_links:
    - from: "gods-eye/backend/app/api/routes.py"
      to: "gods-eye/backend/app/engine/quant_signal_engine.py"
      via: "import QuantSignalEngine, build QuantInputs from historical rows"
      pattern: "from app.engine.quant_signal_engine import"
    - from: "gods-eye/backend/app/engine/quant_backtest.py"
      to: "gods-eye/backend/app/data/historical_store.py"
      via: "await historical_store.get_ohlcv() and get_vix_closes()"
      pattern: "historical_store\\.get_ohlcv"
    - from: "gods-eye/backend/app/engine/quant_backtest.py"
      to: "gods-eye/backend/app/engine/quant_signal_engine.py"
      via: "QuantSignalEngine.compute_quant_score(inputs)"
      pattern: "QuantSignalEngine\\.compute_quant_score"
---

<objective>
Wire QuantSignalEngine into an HTTP endpoint and a fast standalone backtest loop that scans 1 year of stored OHLCV/VIX data without any agent or LLM calls.

Purpose: The pure engine from Plan 01 needs two surfaces: a date-specific live signal endpoint (QUANT-01/03) and a bulk replay loop that proves 1-year performance in under 10 seconds (QUANT-05).
Output: quant_backtest.py + two API routes + Pydantic schemas.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quantitative-signal-engine/10-CONTEXT.md
@.planning/phases/10-quantitative-signal-engine/10-01-SUMMARY.md

@gods-eye/backend/app/engine/quant_signal_engine.py
@gods-eye/backend/app/data/historical_store.py
@gods-eye/backend/app/engine/backtest_engine.py
@gods-eye/backend/app/api/schemas.py
@gods-eye/backend/app/api/routes.py
</context>

<interfaces>
<!-- Contracts from Plan 01 (implemented) -->
```python
@dataclass
class QuantInputs:
    fii_net_cr: float     # FII net flow in crores (proxy: momentum * 1500)
    dii_net_cr: float     # DII net flow (proxy: -momentum * 800)
    pcr: float            # Put/Call ratio (proxy: 0.6 + rsi/100 * 0.8)
    rsi: float            # RSI-14 (0-100)
    vix: float            # Current VIX level
    vix_5d_avg: float     # 5-day average VIX (for direction detection)
    supertrend: str       # "bullish" | "bearish"

@dataclass
class QuantScoreResult:
    total_score: int
    direction: str        # "BUY" | "SELL" | "HOLD"
    buy_points: int
    sell_points: int
    factors: Dict[str, Dict]  # {name: {"points": int, "threshold_hit": bool, "side": str}}
    tier: str             # "strong" | "moderate" | "skip"
    instrument_hint: str  # "NIFTY_CE" | "NIFTY_PE" | "BANKNIFTY_CE" | "BANKNIFTY_PE" | "NONE"

class QuantSignalEngine:
    @staticmethod
    def compute_quant_score(inputs: QuantInputs, instrument: str = "NIFTY") -> QuantScoreResult: ...
```

<!-- From historical_store.py (async, existing singleton) -->
```python
class HistoricalStore:
    async def get_ohlcv(
        self, instrument: str,  # "NIFTY" or "BANKNIFTY"
        from_date: Optional[str] = None,  # "YYYY-MM-DD"
        to_date: Optional[str] = None,    # "YYYY-MM-DD"
    ) -> List[Dict]:  # [{"date", "open", "high", "low", "close", "volume"}] sorted ASC

    async def get_vix_closes(
        self,
        from_date: Optional[str] = None,
        to_date: Optional[str] = None,
    ) -> List[Dict]:  # [{"date", "close"}] sorted ASC

historical_store: HistoricalStore  # module-level singleton
```

<!-- From technical_signals.py (static methods, existing) -->
```python
class TechnicalSignals:
    @staticmethod
    def compute_rsi(closes: List[float], period: int = 14) -> float:
        # returns 50.0 if len(closes) < period+1

    @staticmethod
    def compute_supertrend(rows: List[Dict], period: int = 10, multiplier: float = 3.0) -> str:
        # returns "bullish" or "bearish"
```

<!-- Proxy formulas (from backtest_engine.py lines 377-382 — reuse exactly) -->
```python
momentum_5d_pct = (closes[-1] / closes[-6] - 1.0) * 100  # requires >= 6 closes, else 0.0
fii_net_cr  = momentum_5d_pct * 1500   # -2% move => -3000 Cr proxy
dii_net_cr  = -momentum_5d_pct * 800   # DII absorbs opposite direction
pcr_proxy   = 0.6 + (rsi / 100) * 0.8  # range 0.6 to 1.4
```

<!-- Existing routes.py router (existing pattern) -->
```python
protected_router = APIRouter(prefix="/api", tags=["simulation"], dependencies=[Depends(require_auth)])
# All new endpoints go on protected_router
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuantBacktestEngine — SQLite-only day loop</name>
  <files>gods-eye/backend/app/engine/quant_backtest.py</files>

  <read_first>
    - gods-eye/backend/app/engine/quant_signal_engine.py (QuantInputs, QuantSignalEngine, QuantScoreResult)
    - gods-eye/backend/app/engine/backtest_engine.py (lines 342-420: _build_market_input proxy formula; lines 472-510: _is_correct and _compute_pnl)
    - gods-eye/backend/app/data/historical_store.py (get_ohlcv + get_vix_closes signatures, lines 211-280)
    - gods-eye/backend/app/data/technical_signals.py (compute_rsi + compute_supertrend signatures)
  </read_first>

  <behavior>
    - run_quant_backtest(instrument, from_date, to_date) fetches ALL OHLCV rows once
      (no date filter so RSI/Supertrend lookback has full history before from_date).
    - Then iterates trading days in [from_date, to_date] in a single synchronous Python loop.
    - No async I/O inside the loop.

    Per-day logic:
      closes_up_to = all closes where date <= current_date
      rows_up_to   = all rows where date <= current_date
      rsi          = TechnicalSignals.compute_rsi(closes_up_to)
      supertrend   = TechnicalSignals.compute_supertrend(rows_up_to)
      vix          = vix_lookup[date] or 18.0 if missing
      vix_5d_avg   = mean of up to 5 previous VIX closes (use vix if fewer than 1 available)
      momentum_5d  = (closes_up_to[-1] / closes_up_to[-6] - 1.0) * 100 if len >= 6, else 0.0
      fii_net_cr   = momentum_5d * 1500
      dii_net_cr   = -momentum_5d * 800
      pcr          = 0.6 + (rsi / 100) * 0.8
      score_result = QuantSignalEngine.compute_quant_score(QuantInputs(...), instrument)

      actual_move_pct: (next_day_close / today_close - 1) * 100
        — None for the last day (no next-day data)

      is_correct:
        True  if direction==BUY  and actual_move_pct > 0
        True  if direction==SELL and actual_move_pct < 0
        False if direction==BUY  and actual_move_pct <= 0
        False if direction==SELL and actual_move_pct >= 0
        None  if direction==HOLD or actual_move_pct is None

      pnl_points:
        BUY  + correct   => +50
        BUY  + incorrect => -30
        SELL + correct   => +50
        SELL + incorrect => -30
        HOLD or no data  =>   0

    QuantBacktestRunResult accumulates:
      instrument, from_date, to_date, days: List[QuantBacktestDayResult]
      total_days (all days iterated)
      tradeable_days (days where direction != HOLD and actual_move_pct is not None)
      correct_days (days where is_correct == True)
      win_rate_pct = correct_days / tradeable_days * 100 (None if tradeable_days == 0)
      total_pnl_points = sum of all pnl_points
      elapsed_seconds = wall-clock seconds for the entire run (time.time() start/end)
  </behavior>

  <action>
    Create gods-eye/backend/app/engine/quant_backtest.py.

    Module docstring:
      "Rules-only backtest loop. Reads OHLCV + VIX from SQLite once,
       iterates all trading days without further I/O.
       Zero LLM calls. Zero agent calls.
       Performance target: 250 trading days in under 10 seconds."

    Imports:
      import time
      from dataclasses import dataclass, asdict
      from typing import Dict, List, Optional
      from app.data.historical_store import historical_store
      from app.data.technical_signals import TechnicalSignals
      from app.engine.quant_signal_engine import QuantInputs, QuantSignalEngine

    Define QuantBacktestDayResult as @dataclass:
      date: str
      direction: str
      total_score: int
      tier: str
      buy_points: int
      sell_points: int
      factors: Dict
      actual_move_pct: Optional[float]
      is_correct: Optional[bool]
      pnl_points: float

    Define QuantBacktestRunResult as @dataclass:
      instrument: str
      from_date: str
      to_date: str
      days: List[QuantBacktestDayResult]
      total_days: int
      tradeable_days: int
      correct_days: int
      win_rate_pct: Optional[float]
      total_pnl_points: float
      elapsed_seconds: float

    class QuantBacktestEngine:
      async def run_quant_backtest(
          self, instrument: str, from_date: str, to_date: str
      ) -> QuantBacktestRunResult:
        t0 = time.time()
        all_rows = await historical_store.get_ohlcv(instrument)
        all_vix  = await historical_store.get_vix_closes()

        # Build O(1) lookups
        row_by_date = {r["date"]: r for r in all_rows}
        vix_by_date = {v["date"]: v["close"] for v in all_vix}
        all_dates   = sorted(row_by_date.keys())

        # VIX list for 5d avg computation (sorted)
        vix_dates = [v["date"] for v in all_vix]

        date_range = [d for d in all_dates if from_date <= d <= to_date]
        results: List[QuantBacktestDayResult] = []

        for i, date in enumerate(date_range):
            closes_up_to = [r["close"] for r in all_rows if r["date"] <= date]
            rows_up_to   = [r for r in all_rows if r["date"] <= date]

            rsi        = TechnicalSignals.compute_rsi(closes_up_to)
            supertrend = TechnicalSignals.compute_supertrend(rows_up_to)
            vix        = vix_by_date.get(date, 18.0)

            # 5-day VIX average from previous closes
            prev_vix = [vix_by_date[d] for d in vix_dates if d < date]
            vix_5d_avg = float(sum(prev_vix[-5:]) / len(prev_vix[-5:])) if prev_vix else vix

            momentum_5d = (closes_up_to[-1] / closes_up_to[-6] - 1.0) * 100 \
                          if len(closes_up_to) >= 6 else 0.0

            inputs = QuantInputs(
                fii_net_cr=momentum_5d * 1500,
                dii_net_cr=-momentum_5d * 800,
                pcr=0.6 + (rsi / 100) * 0.8,
                rsi=rsi,
                vix=vix,
                vix_5d_avg=vix_5d_avg,
                supertrend=supertrend,
            )
            sr = QuantSignalEngine.compute_quant_score(inputs, instrument)

            # Next-day actual move
            actual_move_pct: Optional[float] = None
            if i + 1 < len(date_range):
                next_date  = date_range[i + 1]
                next_close = row_by_date[next_date]["close"]
                today_close = closes_up_to[-1]
                actual_move_pct = (next_close / today_close - 1.0) * 100

            is_correct: Optional[bool] = None
            pnl_points = 0.0
            if sr.direction != "HOLD" and actual_move_pct is not None:
                if sr.direction == "BUY":
                    is_correct = actual_move_pct > 0
                else:
                    is_correct = actual_move_pct < 0
                pnl_points = 50.0 if is_correct else -30.0

            results.append(QuantBacktestDayResult(
                date=date, direction=sr.direction, total_score=sr.total_score,
                tier=sr.tier, buy_points=sr.buy_points, sell_points=sr.sell_points,
                factors=sr.factors, actual_move_pct=actual_move_pct,
                is_correct=is_correct, pnl_points=pnl_points,
            ))

        tradeable = [r for r in results if r.direction != "HOLD" and r.actual_move_pct is not None]
        correct   = [r for r in tradeable if r.is_correct]

        return QuantBacktestRunResult(
            instrument=instrument, from_date=from_date, to_date=to_date,
            days=results,
            total_days=len(results),
            tradeable_days=len(tradeable),
            correct_days=len(correct),
            win_rate_pct=(len(correct) / len(tradeable) * 100) if tradeable else None,
            total_pnl_points=sum(r.pnl_points for r in results),
            elapsed_seconds=time.time() - t0,
        )

    quant_backtest_engine = QuantBacktestEngine()  # module-level singleton

    Performance note: closes_up_to uses list comprehension with date comparison per iteration —
    O(N^2) but N=250 so worst case ~62,500 comparisons, well under 1 second in CPython.
    No optimization needed to hit the 10-second target.
  </action>

  <verify>
    <automated>cd /Users/aasish/Desktop/CLAUDE\ PROJECTS/TRD/gods-eye/backend && python -c "import asyncio; from app.engine.quant_backtest import quant_backtest_engine; r = asyncio.run(quant_backtest_engine.run_quant_backtest('NIFTY','2025-01-01','2025-03-31')); print(f'days={r.total_days} elapsed={r.elapsed_seconds:.2f}s pnl={r.total_pnl_points}'); assert r.elapsed_seconds < 10; print('PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - Module imports cleanly: python -c "from app.engine.quant_backtest import quant_backtest_engine; print('ok')"
    - run_quant_backtest returns QuantBacktestRunResult with total_days > 0.
    - elapsed_seconds is less than 10.0 for a 3-month window.
    - factors field on each day is a non-empty dict.
    - tradeable_days + (total_days - tradeable_days) == total_days.
  </acceptance_criteria>

  <done>quant_backtest.py importable, run_quant_backtest returns valid result in under 10 seconds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API schemas and routes for quant signal + quant backtest</name>
  <files>gods-eye/backend/app/api/schemas.py, gods-eye/backend/app/api/routes.py</files>

  <read_first>
    - gods-eye/backend/app/api/schemas.py (existing schemas to follow — append new ones at end)
    - gods-eye/backend/app/api/routes.py (lines 44-52: protected_router pattern; lines 944-984: existing backtest routes to understand registration pattern)
    - gods-eye/backend/app/engine/quant_signal_engine.py (QuantInputs, QuantScoreResult fields)
    - gods-eye/backend/app/engine/quant_backtest.py (QuantBacktestRunResult, QuantBacktestDayResult fields)
    - gods-eye/backend/app/data/historical_store.py (get_ohlcv signature for date endpoint)
  </read_first>

  <behavior>
    New Pydantic schemas in schemas.py:

    QuantFactorSchema:
      points: int
      threshold_hit: bool
      side: Optional[str]

    QuantSignalResponse:
      instrument: str
      date: str
      total_score: int
      direction: str
      tier: str
      buy_points: int
      sell_points: int
      factors: Dict[str, QuantFactorSchema]
      instrument_hint: str

    QuantBacktestDaySchema:
      date: str
      direction: str
      total_score: int
      tier: str
      buy_points: int
      sell_points: int
      actual_move_pct: Optional[float]
      is_correct: Optional[bool]
      pnl_points: float

    QuantBacktestRunResponse:
      instrument: str
      from_date: str
      to_date: str
      total_days: int
      tradeable_days: int
      correct_days: int
      win_rate_pct: Optional[float]
      total_pnl_points: float
      elapsed_seconds: float
      days: List[QuantBacktestDaySchema]

    New routes in routes.py (on protected_router):

    GET /api/signal/quant/{instrument}/{date}
      - instrument: "NIFTY" or "BANKNIFTY"
      - date: "YYYY-MM-DD"
      - Fetches OHLCV rows up to and including date using await historical_store.get_ohlcv(instrument, to_date=date)
      - Fetches VIX closes up to date using await historical_store.get_vix_closes(to_date=date)
      - Computes rsi, supertrend, vix, vix_5d_avg, momentum proxies exactly as quant_backtest.py Task 1 does
      - Calls QuantSignalEngine.compute_quant_score(inputs, instrument)
      - Returns QuantSignalResponse
      - Raises HTTPException(404) if fewer than 15 rows (insufficient for RSI-14)

    POST /api/backtest/quant-run
      Body: {"instrument": str, "from_date": str, "to_date": str}
      - Validates instrument is "NIFTY" or "BANKNIFTY"
      - Validates date format "YYYY-MM-DD"
      - Calls await quant_backtest_engine.run_quant_backtest(instrument, from_date, to_date)
      - Returns QuantBacktestRunResponse (factors omitted from day objects to keep payload small)
      - Raises HTTPException(400) on invalid instrument or date
  </behavior>

  <action>
    Step 1 — Append to gods-eye/backend/app/api/schemas.py:
      Add QuantFactorSchema, QuantSignalResponse, QuantBacktestDaySchema, QuantBacktestRunResponse
      at the bottom of the file. Do NOT modify any existing schemas.

    Step 2 — Append to gods-eye/backend/app/api/routes.py:
      At top imports section, add:
        from app.engine.quant_signal_engine import QuantInputs, QuantSignalEngine
        from app.engine.quant_backtest import quant_backtest_engine, QuantBacktestRunResult
        from app.api.schemas import QuantSignalResponse, QuantBacktestRunResponse, QuantBacktestDaySchema

      Add imports for TechnicalSignals and historical_store if not already present:
        from app.data.historical_store import historical_store
        from app.data.technical_signals import TechnicalSignals

      Append two new route handlers at the bottom of routes.py (before any existing
      "if __name__ == '__main__'" block if present, otherwise at end of file):

      @protected_router.get("/signal/quant/{instrument}/{date}", response_model=QuantSignalResponse)
      async def get_quant_signal(instrument: str, date: str):
          if instrument not in ("NIFTY", "BANKNIFTY"):
              raise HTTPException(status_code=400, detail="instrument must be NIFTY or BANKNIFTY")
          rows = await historical_store.get_ohlcv(instrument, to_date=date)
          if len(rows) < 15:
              raise HTTPException(status_code=404, detail="Insufficient historical data for this date")
          vix_rows = await historical_store.get_vix_closes(to_date=date)
          closes = [r["close"] for r in rows]
          rsi = TechnicalSignals.compute_rsi(closes)
          supertrend = TechnicalSignals.compute_supertrend(rows)
          vix_val = vix_rows[-1]["close"] if vix_rows else 18.0
          prev_vix = [v["close"] for v in vix_rows[:-1]]
          vix_5d_avg = float(sum(prev_vix[-5:]) / len(prev_vix[-5:])) if prev_vix else vix_val
          momentum_5d = (closes[-1] / closes[-6] - 1.0) * 100 if len(closes) >= 6 else 0.0
          inputs = QuantInputs(
              fii_net_cr=momentum_5d * 1500,
              dii_net_cr=-momentum_5d * 800,
              pcr=0.6 + (rsi / 100) * 0.8,
              rsi=rsi, vix=vix_val, vix_5d_avg=vix_5d_avg, supertrend=supertrend,
          )
          sr = QuantSignalEngine.compute_quant_score(inputs, instrument)
          return QuantSignalResponse(
              instrument=instrument, date=date,
              total_score=sr.total_score, direction=sr.direction,
              tier=sr.tier, buy_points=sr.buy_points, sell_points=sr.sell_points,
              factors=sr.factors, instrument_hint=sr.instrument_hint,
          )

      @protected_router.post("/backtest/quant-run", response_model=QuantBacktestRunResponse)
      async def run_quant_backtest(body: dict):
          instrument = body.get("instrument", "NIFTY")
          from_date = body.get("from_date")
          to_date = body.get("to_date")
          if instrument not in ("NIFTY", "BANKNIFTY"):
              raise HTTPException(status_code=400, detail="instrument must be NIFTY or BANKNIFTY")
          if not from_date or not to_date:
              raise HTTPException(status_code=400, detail="from_date and to_date required (YYYY-MM-DD)")
          result = await quant_backtest_engine.run_quant_backtest(instrument, from_date, to_date)
          day_schemas = [
              QuantBacktestDaySchema(
                  date=d.date, direction=d.direction, total_score=d.total_score,
                  tier=d.tier, buy_points=d.buy_points, sell_points=d.sell_points,
                  actual_move_pct=d.actual_move_pct, is_correct=d.is_correct,
                  pnl_points=d.pnl_points,
              )
              for d in result.days
          ]
          return QuantBacktestRunResponse(
              instrument=result.instrument, from_date=result.from_date, to_date=result.to_date,
              total_days=result.total_days, tradeable_days=result.tradeable_days,
              correct_days=result.correct_days, win_rate_pct=result.win_rate_pct,
              total_pnl_points=result.total_pnl_points, elapsed_seconds=result.elapsed_seconds,
              days=day_schemas,
          )

    Use a Pydantic BaseModel for the quant-run request body instead of raw dict if schemas.py
    already defines a pattern for request bodies. Add QuantBacktestRequest(BaseModel) with
    instrument, from_date, to_date fields if that pattern exists; otherwise raw dict is fine.
  </action>

  <verify>
    <automated>cd /Users/aasish/Desktop/CLAUDE\ PROJECTS/TRD/gods-eye/backend && python -c "from app.api.schemas import QuantSignalResponse, QuantBacktestRunResponse; from app.api.routes import protected_router; routes=[r.path for r in protected_router.routes]; assert '/api/signal/quant/{instrument}/{date}' in routes, f'missing quant signal route, got: {routes}'; assert '/api/backtest/quant-run' in routes, 'missing quant-run route'; print('PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - Both QuantSignalResponse and QuantBacktestRunResponse importable from schemas.py.
    - /api/signal/quant/{instrument}/{date} and /api/backtest/quant-run are registered on protected_router.
    - Backend starts without import errors: uvicorn app.main:app --reload (no crash on startup).
    - GET /api/signal/quant/NIFTY/2025-03-01 (with valid auth header) returns JSON with total_score (int), direction, tier, factors.
    - POST /api/backtest/quant-run with {"instrument":"NIFTY","from_date":"2025-01-01","to_date":"2025-12-31"} returns elapsed_seconds < 10.
  </acceptance_criteria>

  <done>
    Both endpoints registered and responding. Backend starts cleanly. 1-year quant backtest
    returns in under 10 seconds (QUANT-05 satisfied). Score endpoint returns per-factor
    breakdown matching QUANT-01/02 requirements.
  </done>
</task>

</tasks>

<verification>
From gods-eye/backend/:

1. python -m pytest app/engine/test_quant_signal_engine.py -v
   (All 20 tests pass — inherited from Plan 01)

2. python -c "from app.engine.quant_backtest import quant_backtest_engine; print('ok')"

3. python -c "
   from app.api.schemas import QuantSignalResponse, QuantBacktestRunResponse
   from app.api.routes import protected_router
   paths = [r.path for r in protected_router.routes]
   assert '/api/signal/quant/{instrument}/{date}' in paths
   assert '/api/backtest/quant-run' in paths
   print('routes ok')
   "

4. Time check (requires data in DB):
   python -c "
   import asyncio, time
   from app.engine.quant_backtest import quant_backtest_engine
   r = asyncio.run(quant_backtest_engine.run_quant_backtest('NIFTY','2024-01-01','2024-12-31'))
   assert r.elapsed_seconds < 10, f'Too slow: {r.elapsed_seconds}s'
   print(f'1-year backtest: {r.total_days} days in {r.elapsed_seconds:.2f}s — PASS')
   "
</verification>

<success_criteria>
1. GET /api/signal/quant/NIFTY/{date} returns QuantSignalResponse with total_score, direction, tier, buy_points, sell_points, factors (per-factor dict).
2. POST /api/backtest/quant-run for 1 year returns elapsed_seconds under 10.0 (QUANT-05).
3. direction is always BUY, SELL, or HOLD — never null or missing.
4. No LLM imports, no Orchestrator imports in quant_backtest.py or quant_signal_engine.py.
5. Backend startup produces no ImportError.
</success_criteria>

<output>
After completion, create .planning/phases/10-quantitative-signal-engine/10-02-SUMMARY.md
</output>
