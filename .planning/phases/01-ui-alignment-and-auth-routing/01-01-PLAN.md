---
phase: 01-ui-alignment-and-auth-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TRD/gods-eye/frontend/src/constants/agents.js
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "A single source-of-truth file exports all agent identifiers, display names, colors, and weights"
    - "Every agent key, label, color, and weight matches the plan spec exactly"
    - "Downstream plans and components can import from this file without reading other files"
  artifacts:
    - path: "TRD/gods-eye/frontend/src/constants/agents.js"
      provides: "Canonical agent definitions: keys, display names, short labels, colors, weights"
      exports:
        - AGENTS (ordered array with id, displayName, shortLabel, color, weight)
        - AGENT_COLORS (object keyed by uppercase id)
        - AGENT_WEIGHTS (object keyed by uppercase id)
        - AGENT_DISPLAY_NAMES (object keyed by uppercase id)
        - AGENT_ORDER (array of uppercase ids in canonical order)
  key_links:
    - from: "TRD/gods-eye/frontend/src/constants/agents.js"
      to: "TRD/gods-eye/frontend/src/utils/colors.js"
      via: "agents.js replaces the agents and agentLabels exports as the canonical source"
      pattern: "export const AGENTS"
---

<objective>
Create a single source-of-truth constants file for all agent definitions that every other component in Phase 1 will import from.

Purpose: The codebase currently scatters agent names, labels, and colors across Settings.jsx, SimulationStream.jsx, AgentDetail.jsx, and utils/colors.js — with inconsistencies. A shared constants file eliminates drift and gives Phase 1 plans a stable contract to build against.

Output: TRD/gods-eye/frontend/src/constants/agents.js with all canonical agent data.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@TRD/gods-eye/frontend/src/utils/colors.js
@TRD/gods-eye/frontend/src/pages/Settings.jsx

<interfaces>
<!-- Existing exports in utils/colors.js that this plan supersedes for agents: -->
```javascript
// Current (to be replaced as canonical source):
export const agents = {
  FII:        '#FF6B6B',
  DII:        '#00E676',
  RETAIL_FNO: '#FFD740',
  ALGO:       '#00D4E0',
  PROMOTER:   '#BB86FC',
  RBI:        '#448AFF',
}
export const agentLabels = {
  FII: 'FII Flows',
  DII: 'DII Strategy',
  RETAIL_FNO: 'Retail F&O',
  ALGO: 'Algo Engine',
  PROMOTER: 'Promoter Desk',
  RBI: 'RBI Policy',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/constants/agents.js with canonical agent definitions</name>
  <files>TRD/gods-eye/frontend/src/constants/agents.js</files>
  <read_first>
    - TRD/gods-eye/frontend/src/utils/colors.js (see current agent colors to preserve hex values)
    - TRD/gods-eye/frontend/src/pages/Settings.jsx (see current agentLabels, agentColors, agentWeights)
    - TRD/gods-eye/frontend/src/components/SimulationStream.jsx (see AGENT_ORDER)
  </read_first>
  <action>
Create a new file TRD/gods-eye/frontend/src/constants/agents.js.

This file is the single source of truth for all agent definitions. Do NOT modify any existing file in this task — only create the new constants file.

The file must export:

1. `AGENT_ORDER` — array of uppercase string IDs in canonical order:
   `['FII', 'DII', 'RETAIL_FNO', 'ALGO', 'PROMOTER', 'RBI']`

2. `AGENTS` — array of agent objects in canonical order, each with:
   - `id`: uppercase key (e.g. 'FII')
   - `displayName`: full display name from plan spec (e.g. 'FII Flows Analyst')
   - `shortLabel`: 3-char label used in compact contexts (e.g. 'FII')
   - `color`: hex color string (preserve from utils/colors.js)
   - `weight`: numeric weight from plan spec (FII=0.30, DII=0.25, RETAIL_FNO=0.15, ALGO=0.10, PROMOTER=0.10, RBI=0.10)

   Exact values:
   ```
   { id: 'FII',        displayName: 'FII Flows Analyst',     shortLabel: 'FII', color: '#FF6B6B', weight: 0.30 }
   { id: 'DII',        displayName: 'DII Strategy Desk',     shortLabel: 'DII', color: '#00E676', weight: 0.25 }
   { id: 'RETAIL_FNO', displayName: 'Retail F&O Desk',       shortLabel: 'RET', color: '#FFD740', weight: 0.15 }
   { id: 'ALGO',       displayName: 'Algo Trading Engine',   shortLabel: 'ALG', color: '#00D4E0', weight: 0.10 }
   { id: 'PROMOTER',   displayName: 'Promoter Desk',         shortLabel: 'PRM', color: '#BB86FC', weight: 0.10 }
   { id: 'RBI',        displayName: 'RBI Policy Desk',       shortLabel: 'RBI', color: '#448AFF', weight: 0.10 }
   ```

3. `AGENT_COLORS` — object mapping uppercase id to hex color (for backward compatibility with existing components that import from utils/colors.js):
   ```javascript
   export const AGENT_COLORS = Object.fromEntries(AGENTS.map(a => [a.id, a.color]))
   ```

4. `AGENT_WEIGHTS` — object mapping uppercase id to numeric weight:
   ```javascript
   export const AGENT_WEIGHTS = Object.fromEntries(AGENTS.map(a => [a.id, a.weight]))
   ```

5. `AGENT_DISPLAY_NAMES` — object mapping uppercase id to displayName:
   ```javascript
   export const AGENT_DISPLAY_NAMES = Object.fromEntries(AGENTS.map(a => [a.id, a.displayName]))
   ```

6. `AGENT_SHORT_LABELS` — object mapping uppercase id to shortLabel:
   ```javascript
   export const AGENT_SHORT_LABELS = Object.fromEntries(AGENTS.map(a => [a.id, a.shortLabel]))
   ```

Add a JSDoc comment at the top of the file:
```
/**
 * Canonical agent definitions — single source of truth for all agent data.
 * Plan spec: FII 0.30, DII 0.25, Retail F&O 0.15, Algo 0.10, Promoter 0.10, RBI 0.10
 * Use these exports in all components. Do NOT hardcode agent names, weights, or colors elsewhere.
 */
```

Do NOT modify utils/colors.js in this task. Other plans in Phase 1 will update their imports independently.
  </action>
  <verify>
    <automated>node -e "const a = require('/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend/src/constants/agents.js'); console.log(Object.keys(a).sort().join(','))" 2>/dev/null || grep -c "export const" "/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend/src/constants/agents.js"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at TRD/gods-eye/frontend/src/constants/agents.js
    - `grep -c "export const" TRD/gods-eye/frontend/src/constants/agents.js` returns 6 (AGENTS, AGENT_ORDER, AGENT_COLORS, AGENT_WEIGHTS, AGENT_DISPLAY_NAMES, AGENT_SHORT_LABELS)
    - `grep "weight: 0.30" TRD/gods-eye/frontend/src/constants/agents.js` matches (FII weight)
    - `grep "weight: 0.25" TRD/gods-eye/frontend/src/constants/agents.js` matches (DII weight)
    - `grep "weight: 0.15" TRD/gods-eye/frontend/src/constants/agents.js` matches (RETAIL_FNO weight)
    - `grep "RETAIL_FNO" TRD/gods-eye/frontend/src/constants/agents.js` matches
    - `grep "'Retail F&O Desk'" TRD/gods-eye/frontend/src/constants/agents.js` matches
    - `grep "'RBI Policy Desk'" TRD/gods-eye/frontend/src/constants/agents.js` matches
    - `grep "AGENT_ORDER" TRD/gods-eye/frontend/src/constants/agents.js` matches
    - utils/colors.js is NOT modified (verify with git diff — no changes to that file)
  </acceptance_criteria>
  <done>File exists with 6 named exports, exact plan-spec weights, plan-spec display names, and correct colors. No existing file is modified.</done>
</task>

</tasks>

<verification>
- `grep -c "export const" TRD/gods-eye/frontend/src/constants/agents.js` returns 6
- `grep "0.30\|0.25\|0.15\|0.10" TRD/gods-eye/frontend/src/constants/agents.js` returns 6 lines (one per agent weight)
- `grep "Retail F&O Desk" TRD/gods-eye/frontend/src/constants/agents.js` returns a match
- No existing file in src/ is modified
</verification>

<success_criteria>
TRD/gods-eye/frontend/src/constants/agents.js exists, exports AGENTS array and 5 derived lookup objects, contains exact plan-spec weights (0.30/0.25/0.15/0.10/0.10/0.10), and uses plan-spec display names for all 6 agents.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-alignment-and-auth-routing/01-01-SUMMARY.md`
</output>
