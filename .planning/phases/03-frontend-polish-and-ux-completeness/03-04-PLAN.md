---
phase: 03-frontend-polish-and-ux-completeness
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - TRD/gods-eye/frontend/src/pages/Welcome.jsx
  - TRD/gods-eye/frontend/src/pages/PaperTrading.jsx
  - TRD/gods-eye/frontend/src/components/AccuracyPanel.jsx
  - TRD/gods-eye/frontend/src/components/FeedbackPanel.jsx
  - TRD/gods-eye/frontend/src/components/ScenarioPanel.jsx
autonomous: true
requirements:
  - FE-07

must_haves:
  truths:
    - "Welcome.jsx uses only palette tokens (surface-*, primary, onSurface*) — no raw hex colors that diverge from the design system"
    - "PaperTrading.jsx uses only palette tokens for all status/state colors — no hardcoded hex that duplicates bull/bear/neutral"
    - "AccuracyPanel and FeedbackPanel use palette token classes, not inline style color overrides for text or backgrounds"
    - "No screen uses a white/light background that breaks the Apple dark theme"
    - "All inline style={{ color: '#hex' }} that duplicate defined palette colors are replaced with Tailwind classes"
  artifacts:
    - path: "TRD/gods-eye/frontend/src/pages/Welcome.jsx"
      provides: "Palette-consistent styling (no off-palette hex colors)"
    - path: "TRD/gods-eye/frontend/src/pages/PaperTrading.jsx"
      provides: "Status colors using bull/bear/neutral Tailwind tokens"
    - path: "TRD/gods-eye/frontend/src/components/AccuracyPanel.jsx"
      provides: "Chart/stat colors using palette tokens where possible"
    - path: "TRD/gods-eye/frontend/src/components/FeedbackPanel.jsx"
      provides: "Feedback colors using bull/bear/neutral palette tokens"
    - path: "TRD/gods-eye/frontend/src/components/ScenarioPanel.jsx"
      provides: "Palette-consistent button and status colors"
  key_links:
    - from: "Tailwind palette"
      to: "All page/component files"
      via: "Tailwind class substitution"
      pattern: "text-bull|text-bear|text-primary|bg-surface"
---

<objective>
Audit every page and component for off-palette colors and replace them with the defined Tailwind design system tokens. The theme is always dark (Apple dark + Claude warm palette) — the goal is consistency, not adding a toggle.

Purpose: FE-07 — dark mode theme must be consistent across all screens using the established palette.
Output: Off-palette hex colors in Welcome, PaperTrading, AccuracyPanel, FeedbackPanel, ScenarioPanel replaced with correct Tailwind tokens.
</objective>

<execution_context>
@/Users/aasish/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aasish/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- Canonical palette from tailwind.config.js — use THESE tokens. -->

Surface layers: `bg-surface-0` (#0A0A0F), `bg-surface-1` (#0F1117), `bg-surface-2` (#161922), `bg-surface-3` (#1C1F2B), `bg-surface-4` (#242836)
Primary: `text-primary` (#00D4E0), `bg-primary/10`, `border-primary/20`, `text-primary-muted`, `text-primary-dim`
Bull (green): `text-bull` (#00E676), `bg-bull/10`, `border-bull/20`, `text-bull-bright` (#00FF88), `text-bull-muted`
Bear (red): `text-bear` (#FF1744), `bg-bear/10`, `border-bear/20`, `text-bear-bright` (#FF3D71), `text-bear-muted`
Neutral (amber): `text-neutral` (#FFC107), `bg-neutral/10`, `border-neutral/20`, `text-neutral-bright` (#FFD740)
Text: `text-onSurface` (#E8ECF1), `text-onSurfaceMuted` (#8B95A5), `text-onSurfaceDim` (#505A6B)

KNOWN off-palette patterns to fix:
- `#00E676` → already IS bull DEFAULT, so `text-bull` or `style={{ color: '#00E676' }}` can stay or become `text-bull`
- `#FF1744` → `text-bear`
- `#FFC107` → `text-neutral`
- `#FF3D71` → `text-bear-bright`
- `#FFD740` → `text-neutral-bright`
- Any hex not in the palette above (e.g., arbitrary greens like `#4CAF50`, blues like `#2196F3`) → map to nearest palette token
- `color: 'white'` or `color: '#fff'` → `text-onSurface`
- `backgroundColor: '#0A0A0F'` → `bg-surface-0`

Agent colors (inline styles are acceptable for agent-specific coloring since they come from AGENTS constant — do NOT change those).

Files to audit (Wave 2 depends on 01-03, so the new Skills.jsx from plan 03 was written with palette tokens already):
- Welcome.jsx — the unauthenticated landing page
- PaperTrading.jsx — graduation criteria status badges
- AccuracyPanel.jsx — accuracy chart colors
- FeedbackPanel.jsx — feedback weight display colors
- ScenarioPanel.jsx — scenario type badges and buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix Welcome.jsx and ScenarioPanel.jsx palette consistency</name>
  <files>
    TRD/gods-eye/frontend/src/pages/Welcome.jsx
    TRD/gods-eye/frontend/src/components/ScenarioPanel.jsx
  </files>
  <read_first>
    TRD/gods-eye/frontend/src/pages/Welcome.jsx
    TRD/gods-eye/frontend/src/components/ScenarioPanel.jsx
  </read_first>
  <action>
Read both files fully first.

**Welcome.jsx audit:**
Scan for:
1. Any `style={{ color: '#hex' }}` or `style={{ backgroundColor: '#hex' }}` where the hex is not an agent color (agent colors from AGENTS constant are acceptable).
2. Any Tailwind classes using colors not in the design system (e.g., `text-green-400`, `text-red-500`, `bg-blue-900` — these are Tailwind defaults, not our palette).
3. Any `text-white` or `bg-black` (should be `text-onSurface` and `bg-surface-0`).

For each violation found:
- Replace `text-green-*` → `text-bull`
- Replace `text-red-*` → `text-bear`
- Replace `text-yellow-*` or `text-amber-*` → `text-neutral` or `text-neutral-bright`
- Replace `text-blue-*` → `text-primary`
- Replace `text-white` → `text-onSurface`
- Replace `bg-black` or `bg-gray-900` → `bg-surface-0`
- Replace `bg-gray-800` → `bg-surface-1` or `bg-surface-2`
- Keep `style={{ color: agentColor }}` patterns — these use the AGENTS constant and are intentional.

If Welcome.jsx already uses only design system tokens (surface-*, primary, onSurface*, bull, bear, neutral), note that and make no changes to it.

**ScenarioPanel.jsx audit:**
Same scan. Focus on scenario type badge colors (BULLISH/BEARISH/NEUTRAL labels) and button states. Replace any raw hex with appropriate tokens:
- Bullish scenario color → `text-bull`, `bg-bull/10`, `border-bull/20`
- Bearish scenario color → `text-bear`, `bg-bear/10`, `border-bear/20`
- Neutral → `text-neutral`, `bg-neutral/10`, `border-neutral/20`

Do NOT change any agent-specific colors that come from the agents constant/utility.
  </action>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend" && node -e "
const fs = require('fs');
const files = ['src/pages/Welcome.jsx', 'src/components/ScenarioPanel.jsx'];
let warnings = [];
for (const f of files) {
  const c = fs.readFileSync(f, 'utf8');
  // Check for off-palette Tailwind defaults (not in our design system)
  const offPalette = [/text-green-\d+/, /text-red-\d+/, /text-blue-\d+/, /text-yellow-\d+/, /text-white\b/, /bg-black\b/, /bg-gray-\d+/];
  for (const re of offPalette) {
    if (re.test(c)) warnings.push(f + ': found ' + re.toString());
  }
}
if (warnings.length > 0) { console.warn('WARNINGS (off-palette):', warnings.join(', ')); }
else { console.log('OK: No off-palette default Tailwind colors found'); }
"</automated>
  </verify>
  <acceptance_criteria>
    - Welcome.jsx and ScenarioPanel.jsx contain no `text-green-*`, `text-red-*`, `text-blue-*`, `text-yellow-*`, `text-white`, `bg-black`, or `bg-gray-*` Tailwind default classes
    - Agent-specific inline style colors (from constants/agents.js) are preserved as-is
    - Direction-based colors (bull/bear/neutral) use the correct design system tokens
  </acceptance_criteria>
  <done>Welcome.jsx and ScenarioPanel.jsx use only design system palette tokens for all non-agent colors.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix PaperTrading, AccuracyPanel, FeedbackPanel palette consistency</name>
  <files>
    TRD/gods-eye/frontend/src/pages/PaperTrading.jsx
    TRD/gods-eye/frontend/src/components/AccuracyPanel.jsx
    TRD/gods-eye/frontend/src/components/FeedbackPanel.jsx
  </files>
  <read_first>
    TRD/gods-eye/frontend/src/pages/PaperTrading.jsx
    TRD/gods-eye/frontend/src/components/AccuracyPanel.jsx
    TRD/gods-eye/frontend/src/components/FeedbackPanel.jsx
  </read_first>
  <action>
Read all three files fully.

**PaperTrading.jsx audit:**
The graduation criteria panel uses status colors for PASS/FAIL/N/A states. Ensure:
- PASS/met criteria use `text-bull` (not `text-green-400` or raw hex `#00E676` as a className — inline style for dynamic values is OK)
- FAIL criteria use `text-bear`
- N/A / insufficient data use `text-onSurfaceDim`
- Progress bar fill colors that are dynamic (computed from a value) can remain as inline styles — only fix hardcoded class-based colors

**AccuracyPanel.jsx audit:**
This component likely shows accuracy stats for agents. Scan for:
- Off-palette Tailwind color classes (same list as Task 1)
- Any `className="text-green-*"` patterns → `text-bull`
- Any accuracy percent thresholds that set color by value — if they already use inline `style={{ color: ... }}` with `'#00E676'` / `'#FF1744'` / `'#FFC107'`, leave them (they're dynamic computed values, not hardcoded design violations)
- Hardcoded class names like `className="text-green-400"` → fix to `className="text-bull"`

**FeedbackPanel.jsx audit:**
Same scan. Feedback weights typically show positive/negative movement. Ensure:
- Positive feedback → `text-bull` not `text-green-*`
- Negative feedback → `text-bear` not `text-red-*`
- If a component uses `style={{ color: n >= 0 ? '#00E676' : '#FF1744' }}` that is acceptable (dynamic, matches bull/bear values exactly)

For ALL three files:
- After fixing any class violations, verify each file still correctly styles its content
- If a file has NO violations, note that in a comment at the top of your changes (or just leave the file unchanged — do not touch working files unnecessarily)
  </action>
  <verify>
    <automated>cd "/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend" && node -e "
const fs = require('fs');
const files = [
  'src/pages/PaperTrading.jsx',
  'src/components/AccuracyPanel.jsx',
  'src/components/FeedbackPanel.jsx',
];
let warnings = [];
for (const f of files) {
  if (!fs.existsSync(f)) { warnings.push(f + ': file not found'); continue; }
  const c = fs.readFileSync(f, 'utf8');
  const offPalette = [/text-green-\d+/, /text-red-\d+/, /text-blue-\d+/, /text-yellow-\d+/, /text-white\b(?!space)/, /bg-black\b/, /bg-gray-\d+/];
  for (const re of offPalette) {
    if (re.test(c)) warnings.push(f + ': found ' + re.toString());
  }
}
if (warnings.length > 0) { console.warn('PALETTE WARNINGS:', warnings.join(' | ')); }
else { console.log('OK: All three files use design system palette tokens'); }
" && npm run build 2>&1 | grep -c "error" && echo "check build"</automated>
  </verify>
  <acceptance_criteria>
    - PaperTrading.jsx graduation criteria colors use `text-bull` / `text-bear` / `text-onSurfaceDim` tokens, not `text-green-*` or `text-red-*`
    - AccuracyPanel.jsx and FeedbackPanel.jsx contain no off-palette default Tailwind color classes
    - Dynamic inline styles that compute color from values (e.g., `color: value > 0 ? '#00E676' : '#FF1744'`) are acceptable and left unchanged
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>
    PaperTrading, AccuracyPanel, and FeedbackPanel pages all use the defined dark theme palette consistently. No default Tailwind color classes (text-green-*, text-red-*, bg-gray-*) remain. The Apple dark + Claude warm palette is consistent across all screens.
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend" && npm run build 2>&1 | tail -10
```
Build must succeed. Then spot-check with a visual grep:
```bash
grep -rn "text-green-\|text-red-\|text-white\b\|bg-black\|bg-gray-" "/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend/src/pages/" "/Users/aasish/Desktop/CLAUDE PROJECTS/TRD/TRD/gods-eye/frontend/src/components/" 2>/dev/null | grep -v ".test.\|__tests__"
```
This should return empty (zero matches) for all audited files.
</verification>

<success_criteria>
1. `npm run build` exits 0
2. No `text-green-*`, `text-red-*`, `text-blue-*`, `text-yellow-*`, `text-white`, `bg-black`, `bg-gray-*` classes in the five audited files
3. Dynamic inline style={{ color: '#hex' }} patterns using bull/bear/neutral values are left in place
4. Agent color inline styles from constants/agents.js are left in place
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-polish-and-ux-completeness/03-04-SUMMARY.md`
</output>
